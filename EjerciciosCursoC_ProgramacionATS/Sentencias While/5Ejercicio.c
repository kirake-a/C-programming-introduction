/* Elaborar el programa para leer una seuencia no determinada de numeros enteros (finalizara la lectura cuando el ultimo numero sea igual a 0) 
y calcular e imprimir el menor numero, el numero mayor y el promedio de los numeros. */

#include <stdio.h>

int main()
{
    float n, sumatoria = 0, promedio;

    while(n != 0)
    {
        printf("Digite un valor: ");
        scanf("%f", &n);


    }

    

    return 0;
}